import fs = require('fs-extra-promise');
import * as pathMod from 'path';
import del = require('del');

async function test() {
  try {
    const data = await fs.readJsonAsync('ddsd');
  }
  catch (err) {

  }
  finally {

  }
}
